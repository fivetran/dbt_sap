name: 'sap_integration_tests'
version: '0.7.0'

profile: 'integration_tests'
config-version: 2

vars:
  sap_schema: sap_integration_tests
  sap:
    sap_bkpf_identifier: "sap_bkpf_data"
    sap_bsad_bck_identifier: "sap_bsad_bck_data"
    sap_bsak_bck_identifier: "sap_bsak_bck_data"
    sap_bseg_identifier: "sap_bseg_data"
    sap_coep_identifier: "sap_coep_data"
    sap_cosp_bak_identifier: "sap_cosp_bak_data"
    sap_faglflexa_identifier: "sap_faglflexa_data"
    sap_faglflext_identifier: "sap_faglflext_data"
    sap_kna1_identifier: "sap_kna1_data"
    sap_lfa1_identifier: "sap_lfa1_data"
    sap_mara_identifier: "sap_mara_data"
    sap_pa0000_identifier: "sap_pa0000_data"
    sap_pa0001_identifier: "sap_pa0001_data"
    sap_pa0007_identifier: "sap_pa0007_data"
    sap_pa0008_identifier: "sap_pa0008_data"
    sap_pa0031_identifier: "sap_pa0031_data"
    sap_prps_identifier: "sap_prps_data"
    sap_ska1_identifier: "sap_ska1_data"
    sap_t001_identifier: "sap_t001_data"
    sap_t503_identifier: "sap_t503_data"
    sap_t880_identifier: "sap_t880_data"
    sap_dd07l_identifier: "sap_dd07l_data"
    sap_dd07t_identifier: "sap_dd07t_data"
    sap_ekko_identifier: "sap_ekko_data"
    sap_makt_identifier: "sap_makt_data"
    sap_t001w_identifier: "sap_t001w_data"
    sap_t024_identifier: "sap_t024_data"
    sap_t024e_identifier: "sap_t024e_data"
    sap_t134_identifier: "sap_t134_data"
    sap_t134t_identifier: "sap_t134t_data"
    sap_t161_identifier: "sap_t161_data"
    sap_t161t_identifier: "sap_t161t_data"
    sap_tvag_identifier: "sap_tvag_data"
    sap_tvagt_identifier: "sap_tvagt_data"
    sap_eket_identifier: "sap_eket_data"
    sap_ekbe_identifier: "sap_ekbe_data"
    sap_ekpo_identifier: "sap_ekpo_data"
    sap_vbak_identifier: "sap_vbak_data"
    sap_vbap_identifier: "sap_vbap_data"
    sap_vbuk_identifier: "sap_vbuk_data"
    sap_vbup_identifier: "sap_vbup_data"
    sap_acdoca_identifier: "sap_acdoca_data"
    sap_finsc_cmp_versnd_identifier: "sap_finsc_cmp_versnd_data"
    sap_finsc_ld_cmp_identifier: "sap_finsc_ld_cmp_data"
    sap_finsc_ledger_rep_identifier: "sap_finsc_ledger_rep_data"
    sap_marc_identifier: "sap_marc_data"
    sap_matdoc_identifier: "sap_matdoc_data"
    sap_matdoc_extract_identifier: "sap_matdoc_extract_data"
    sap_mchb_identifier: "sap_mchb_data"
    sap_mchp_identifier: "sap_mchp_data"
    sap_sapsll_clsnr_identifier: "sap_sapsll_clsnr_data"
    sap_sapsll_maritc_identifier: "sap_sapsll_maritc_data"
    sap_sapsll_nosca_identifier: "sap_sapsll_nosca_data"
    sap_sapsll_tunos_identifier: "sap_sapsll_tunos_data"
    sap_t000_identifier: "sap_t000_data"
    sap_t006_identifier: "sap_t006_data"
    sap_tj01_identifier: "sap_tj01_data"
    sap_tka01_identifier: "sap_tka01_data"

models:
  +persist_docs:
    relation: true
    columns: "{{ false if target.type in ('databricks') else true }}"
  +schema: "sap_{{ var('directed_schema','dev') }}"

dispatch:
  - macro_namespace: dbt_utils
    search_order: ['spark_utils', 'dbt_utils']

seeds:
  +docs:
    show: False
  sap_integration_tests:
    +column_types:
      _fivetran_rowid: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
      _fivetran_synced: timestamp
      "*": "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"

    sap_acdoca_data:
      +column_types:
        acrrefobj_id: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
    sap_sapsll_maritc_data:
      +column_types:
        crea_date_time: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        lchg_date_time: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
    sap_bkpf_data:
      +column_types:
        aedat: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        awkey: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        belnr: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        bldat: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        budat: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        cpudt: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        cputm: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        ctxkrs: numeric
        frath: numeric
        kursf: numeric
        kurs2: numeric
        kurs3: numeric
        kursx: numeric
        kur2x: numeric
        kur3x: numeric
        kzkrs: numeric
        reindat: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        stodt: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        txkrs: numeric
        upddt: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        wwert: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"

    sap_bseg_data:
      +column_types:
        _dataaging: "date"
        abper: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        absbt: numeric
        agzei: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        altkt: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        anfae: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        anfbj: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        aplzl: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        aufpl: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        augcp: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        augdt: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        auggj: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        bdiff: numeric
        bdif2: numeric
        bdif3: numeric
        belnr: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        blnbt: numeric
        blnpz: numeric
        bonfb: numeric
        bpmng: numeric
        bschl: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        bualt: numeric
        bukrs: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        buzei: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        bzdat: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        dabrz: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        disbj: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        disbz: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        dmbe2: numeric
        dmb21: numeric
        dmb22: numeric
        dmb23: numeric
        dmbe3: numeric
        dmb31: numeric
        dmb32: numeric
        dmb33: numeric
        dmbt1: numeric
        dmbt2: numeric
        dmbt3: numeric
        dmbtr: numeric
        dtws1: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        dtws2: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        dtws3: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        dtws4: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        ebelp: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        erfmg: numeric
        eten2: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        fdtag: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}" 
        fdwbt: numeric
        fipos: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}" 
        fkber: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}" 
        fkber_long: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}" 
        fkont: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        fmxdocln: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        fmxyear: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        fwbas: numeric
        fwzuz: numeric
        gbetr: numeric
        gjahr: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        gsber: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        hkont: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        hwbas: numeric
        hwzuz: numeric
        kblpos: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        klibt: numeric
        kokrs: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        kostl: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        kzbtr: numeric
        kursr: numeric
        linfv: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        lnran: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        lokkt: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        madat: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        mandt: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        manst: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        menge: numeric
        mwst2: numeric
        mwst3: numeric
        mwsts: numeric
        navfw: numeric
        navhw: numeric
        navh2: numeric
        navh3: numeric
        nebtr: numeric
        nprei: numeric
        obzei: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        paobjnr: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        pasubnr: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        penfc: numeric
        penlc1: numeric
        penlc2: numeric
        penlc3: numeric
        peinh: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        pernr: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        perop_beg: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        perop_end: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        posn2: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        popts: numeric
        ppdiff: numeric
        ppdif2: numeric
        ppdif3: numeric
        prctr: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        prodper: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        projk: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        pswbt: numeric
        pyamt: numeric
        qbshb: numeric
        qsfbt: numeric
        qsshb: numeric
        rebzj: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        rebzz: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        rdiff: numeric
        rdif2: numeric
        rdif3: numeric
        rewrt: numeric
        rewwr: numeric
        rfzei: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        rpacq: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        samnr: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        sctax: numeric
        skfbt: numeric
        sknto: numeric
        sknt2: numeric
        sknt3: numeric
        sttax: numeric
        taxps: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        txbfw: numeric
        txbhw: numeric
        txbh2: numeric
        txbh3: numeric
        txdat: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        txgrp: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        valut: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        vrsdt: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        wmwst: numeric
        wrbt1: numeric
        wrbt2: numeric
        wrbt3: numeric
        wrbtr: numeric
        wskto: numeric
        zbd1p: numeric
        zbd1t: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        zbd2p: numeric
        zbd2t: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        zbd3t: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        zekkn: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        zfbdt: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        zolld: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        zuonr: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"

    sap_dd07l_data:
      +column_types:
        domname: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        domvalue_l: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        as4vers: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"

    sap_dd07t_data:
      +column_types:
        domname: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        domvalue_l: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        as4vers: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        ddlanguage: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        ddtext: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"

    sap_ekbe_data:
      +column_types:
        dmbtr: numeric
        dmbtr_pop: numeric
        retamt_fc: numeric
        retamt_lc: numeric
        retamtp_fc: numeric
        retamtp_lc: numeric
        wrbtr: numeric
        wrbtr_pop: numeric

    sap_eket_data:
      +column_types:
        otb_res_value: numeric
        otb_spec_value: numeric
        otb_value: numeric

    sap_ekko_data:
      +column_types:
        dpamt: numeric
        dppct: numeric
        ktwrt: numeric
        otb_res_value: numeric
        otb_spec_value: numeric
        otb_value: numeric
        rlwrt: numeric
        mandt: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        ebeln: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        bukrs: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        bstyp: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        bsart: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        lifnr: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        aedat: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        bedat: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        kdatb: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        kdate: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        bwbdt: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        angdt: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        bnddt: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        gwldt: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        ihran: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        dpdat: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        release_date: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        eq_eindt: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"

    sap_ekpo_data:
      +column_types:
        abdat: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        abeln: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        abelp: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        abftz: numeric
        abmng: numeric
        abskz: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        abueb: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        adrn2: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        adrnr: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        advcode: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        aedat: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        afnam: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        agdat: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        agmem: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        aktnr: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        anfnr: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        anfps: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        anzpu: numeric
        anzsn: numeric
        apoms: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        arsnr: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        arsps: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        attyp: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        aurel: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        banfn: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        bednr: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        berid: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        blk_reason_id: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        blk_reason_txt: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        bnfpo: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        bonba: numeric
        bonus: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        bprme: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        bpumn: numeric
        bpumz: numeric
        brgew: numeric
        brtwr: numeric
        bsgru: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        bstae: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        bstyp: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        budget_pd: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        bukrs: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        bwtar: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        bwtty: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        ccomp: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        chg_fplnr: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        chg_srv: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        cmpl_dlv_itm: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        cnfm_qty: numeric
        cons_order: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        cqu_sar: numeric
        cuobj: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        diff_invoice: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        disub_kunnr: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        disub_owner: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        disub_posnr: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        disub_pspnr: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        disub_sobkz: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        disub_vbeln: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        dpamt: numeric
        dpdat: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        dppct: numeric
        dptyp: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        drdat: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        druhr: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        drunr: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        ean11: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        ebeln: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        ebelp: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        ebon2: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        ebon3: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        ebonf: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        effwr: numeric
        eglkz: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        ehtyp: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        eildt: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        ekkol: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        elikz: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        ematn: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        emlif: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        emnfr: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        empst: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        erekz: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        etdrk: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        etfz1: numeric
        etfz2: numeric
        evers: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        excpe: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        exlin: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        exsnr: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        ext_rfx_item: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        ext_rfx_number: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        ext_rfx_system: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        fabkz: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        ffzhi: numeric
        fipos: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        fiscal_incentive: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        fiscal_incentive_id: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        fistl: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        fixmg: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        fkber: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        fls_rsto: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        fmfgus_key: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        fplnr: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        fsh_atp_date: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        fsh_collection: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        fsh_grid_cond_rec: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        fsh_item: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        fsh_item_group: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        fsh_psm_pfm_split: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        fsh_season: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        fsh_season_year: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        fsh_ss: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        fsh_theme: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        fsh_transaction: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        fsh_vas_prnt_id: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        fsh_vas_rel: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        geber: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        gewei: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        gnetwr: numeric
        grant_nbr: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        handoverloc: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        hvr_change_time: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        hvr_is_deleted: numeric
        idnlf: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        inco1: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        inco2: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        inco2_l: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        inco3_l: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        infnr: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        insmk: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        insnc: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        iprkz: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        itcons: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        iuid_relevant: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        j_1aidatep: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        j_1aindxp: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        j_1bindust: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        j_1bmatorg: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        j_1bmatuse: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        j_1bnbm: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        j_1bownpro: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        kanba: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        kblnr: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        kblpos: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        knttp: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        ko_gsber: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        ko_pargb: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        ko_pprctr: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        ko_prctr: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        kolif: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        konnr: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        ktmng: numeric
        ktpnr: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        kunnr: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        kzabs: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        kzbws: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        kzfme: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        kzkfg: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        kzstu: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        kztlf: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        kzvbr: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        kzwi1: numeric
        kzwi2: numeric
        kzwi3: numeric
        kzwi4: numeric
        kzwi5: numeric
        kzwi6: numeric
        labnr: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        lblkz: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        lebre: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        lewed: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        lfret: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        lgort: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        lmein: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        loekz: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        ltsnr: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        mahn1: numeric
        mahn2: numeric
        mahn3: numeric
        mahnz: numeric
        mandt: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        manual_tc_reason: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        matkl: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        matnr: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        meins: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        menge: numeric
        meprf: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        mfrgr: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        mfrnr: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        mfrpn: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        mfzhi: numeric
        mhdrz: numeric
        mlmaa: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        mprof: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        mrpind: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        mtart: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        mwskz: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        navnw: numeric
        netpr: numeric
        netwr: numeric
        nfabd: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        nlabd: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        notkz: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        novet: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        nrfhg: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        ntgew: numeric
        oia_baselo: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        oia_ipmvat: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        oia_spltiv: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        oibasprod: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        oic_adestn: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        oic_aorgin: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        oic_dcityc: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        oic_dcounc: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        oic_dland1: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        oic_dregio: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        oic_lifnr: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        oic_mot: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        oic_ocityc: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        oic_ocounc: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        oic_oland1: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        oic_oregio: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        oic_pbatch: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        oic_pdestn: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        oic_porgin: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        oic_ptrip: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        oic_truckn: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        oicertf1: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        oicertf1_gi: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        oid_extbol: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        oid_miscdl: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        oidatfm1: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        oidatfm1_gi: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        oidatto1: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        oidatto1_gi: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        oiedbal: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        oiedbal_gi: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        oiedbalm: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        oiedbalm_gi: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        oiedok: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        oiedok_gi: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        oiexgnum: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        oiexgtyp: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        oiextnr: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        oifeech: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        oifeedt: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        oifeetot: numeric
        oiferp: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        oiftind: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        oighndl: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        oih_folqty: numeric
        oih_folqty_gi: numeric
        oih_lcfol: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        oih_lcfol_gi: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        oih_licin: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        oih_licin_gi: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        oih_lictp: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        oih_lictp_gi: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        oihantyp: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        oihantyp_gi: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        oiinex: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        oiinex_gi: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        oiitmnr: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        oimatcyc: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        oinetcyc: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        oioilcon: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        oipipeval: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        oipricie: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        oipriop: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        oisbrel: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        oitaxcon: numeric
        oitaxfrom: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        oitaxgrp: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        oitaxgrp_gi: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        oitaxto: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        oitrind: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        oitrkjr: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        oitrknr: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        oitxcon1: numeric
        oitxcon2: numeric
        oitxcon3: numeric
        oitxcon4: numeric
        oitxcon5: numeric
        oitxcon6: numeric
        oiumbar: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        oivatf: numeric
        oivath: numeric
        packno: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        peinh: numeric
        plifz: numeric
        pol_id: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        prdat: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        prio_req: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        prio_urg: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        prsdr: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        pstyp: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        punei: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        put_back: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        rdprf: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        reason_code: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        ref_item: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        refsite: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        repos: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        reslo: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        retpc: numeric
        retpo: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        revlv: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        saisj: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        saiso: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        satnr: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        schpr: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        sernp: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        serru: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        sf_txjcd: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        sgt_rcat: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        sgt_scat: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        sikgr: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        sktof: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        sobkz: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        source_id: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        source_key: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        spe_abgru: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        spe_chng_sys: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        spe_cq_ctrltype: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        spe_cq_nocq: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        spe_crm_fkrel: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        spe_crm_ref_item: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        spe_crm_ref_so: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        spe_crm_so: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar'